# Code-review findings

A full-codebase review run on **2026-07-26** at `f02101f`, one reviewer per domain plus
cross-cutting passes. This file is the working list: every item is either **done**, **dismissed**
with a reason, or still open. It is *not* a changelog — once the list is empty this file goes away
and the outcomes live in the git log. Genuinely deferred product work stays in `open-tasks.md`.

## Provenance and its limits

- **12 of 21 planned reviewers finished** before the run was stopped. Covered: `@shared`,
  `commlink`, `tracking`, `office-time`, `notifications`, `barcode`, `tasks`, `cash`, `trackplay`,
  `settings`, `geist`, plus a quantitative measurement pass over the whole repo.
- **Not covered by a reviewer:** the `groceries` domain, the shell/routing layer, and the
  cross-cutting axis passes for theming, i18n/a11y in depth, build/bundle, and NgRx-across-domains.
  Absence of findings there means nobody looked, not that it is clean.
- **CR-156…160 are not from the run.** CR-156…159 came out of a later hand spot-check of the
  static-asset wiring while answering "what is `public/` for" — a first sample of the unreviewed
  shell/build layer, and evidence that the gap above is worth closing rather than assumed clean.
  CR-160 came out of the design discussion behind CR-016 (why the `canSave` bug class exists at
  all), not from a reviewer.
- **No adversarial verification pass ran.** Every item below is a single reviewer's claim with
  file:line evidence. Each one is re-checked against the source before it is acted on; several have
  already been dismissed on that basis (see the dismissed entries, which record why).

## Progress

**All CRITICAL and HIGH items are closed.** ~28 of 155 actioned, across
`8717cf2 b7c482b 2f529f0 68f37c4 1b9ad86 a8718ed af80396 a0e656c bcc2cbb 0f1897b 0a01f7b 98b6419`.
Suite 875 → 991 passing.

**One thing to pick up first:** `tracking.effects.spec.ts` in the working tree is
flaky and deliberately uncommitted — see CR-024.
Every one was re-verified against the source first; three turned out to need a
different fix than the reviewer proposed, and those reasons are recorded inline.

The remaining items are MEDIUM and LOW. They are being worked in sweeps grouped
by kind (dead code, i18n namespacing, comment drift, barrel trimming, a11y)
rather than one at a time, because most are one-line changes in the same files.


**Session 2 (non-trivial batch)** — `c45ee62 b554b41 61dd01e 4e7c9da 78f90b1 256f1ed 7271082`:
share sheet behind a port (killed the flaky spec at its cause), stale list update
no longer rewrites another row's id, summary docs versioned + `loadPrefixed` no
longer inflates the whole database, trackplay stops persisting the undo snapshot
and rewriting on unchanged cells, geist probe/session lifecycle, OS reminder copy through i18n, office-time period
stats in one pass. Suite at 1021.

## Status legend

- `- [ ]` open
- `- [x] … **DONE** (<sha>)` — fixed and committed
- `- [x] … **DISMISSED** — <reason>` — checked against the source and not worth acting on, or
  already true, or a deliberate decision

## Scorecard at the time of review

| Axis | /10 |
|---|---|
| Architecture & boundaries | 7.5 |
| Simplicity vs over-abstraction | 7.0 |
| State management | 6.6 |
| Naming & readability | 6.6 |
| Testing | 5.6 |
| Correctness & robustness | 5.4 |

Measured facts behind those numbers: 1,055 functions averaging 6.77 LOC (79% ≤10 LOC, exactly one
>50); largest file 388 LOC; 0.45% TypeScript duplication vs 7.61% in templates; zero `any`, zero
`@ts-ignore`, zero TODO/FIXME; two justified `eslint-disable` lines; all five gates green.

## CRITICAL

- [x] **CR-001** **DONE** (b7c482b) — validated: `src/assets` + `public` are the only asset roots, neither has `holidays/`, never committed on any branch. Replaced the fetch with `berlinHolidaysFor`; the destructive `loadHolidaysFailure` arm is gone with it. Fixture follow-up is deferred by decision in `open-tasks.md`. `office-time` · `office-time/data/effects/office-time.effects.ts:119` — The holidays feature fetches an asset that does not exist in the repo, so it fails silently on every page entry and eventually wipes the persisted holiday map.
      _Fix:_ Either ship the holiday assets (and make the missing-year case explicit — `loadHolidaysFailure` should keep the previously loaded/persisted map rather than clearing it, and should surface a `NotificationsActions.toast`), or drop the holidays feature and the `holidays` slice field. At minimum, stop `loadHolidaysFailure` from destroying state, and add the fetched year's file existence to the gate. _(medium)_

## HIGH

- [x] **CR-002** **PARTLY DONE** (af80396) — thresholds added and CI now runs `test:coverage`. The denominator itself is **not** fixed: `coverage.include` does not compose with `@angular/build:unit-test` (the builder runs specs from compiled output, so an include glob re-instruments the sources as a separate uncovered set and the report collapses to 0%). Measured and documented in `vitest.config.ts`; reopened as CR-002b. `repo` · `vitest.config.ts:20` — Coverage reporting silently excludes 112 of 381 source files (29.4%, 6,594 LOC) from the denominator, inflating the headline number from ≈57% to 77.09%.
      _Fix:_ Add `coverage: { include: ['src/app/**/*.ts'], exclude: ['**/*.spec.ts', '**/testing/**'], thresholds: { lines: 55, statements: 50 } }` to vitest.config.ts so unloaded files score 0, then ratchet the thresholds upward. Add the coverage run to .github/workflows/ci.yml. _(small)_
- [x] **CR-003** **PARTLY DONE / DEFERRED** — the four money-path bugs living in this untested seam are fixed and specced (2f529f0). The page/modal component specs themselves (~1,138 LOC across 9 files) are a sizeable piece of new test authoring, and you are already adding facade specs in parallel; scoped out of this review list and recorded in `open-tasks.md` instead of being half-done here. `repo` · `cash/feature/cash-account-page/cash-account.page.ts:1` — The cash domain has 1,917 LOC with zero test coverage — nine page/modal/picker components, including the two highest-churn untested files in the repo.
      _Fix:_ Add component-class specs (TestBed.createComponent(...).componentInstance + provideMockStore, per the repo's stated pattern) for at least cash-account.page.ts, cash-rules.page.ts, transfer-modal and reconcile-modal, and extend e2e/cash/ to cover /#/cash/rules and /#/cash/report. _(medium)_
- [x] **CR-004** **DONE** (8717cf2) `shared` · `@shared/data/effects/persisted-slice.effects.factory.ts:63` — A failed storage read is reported as `loaded(null)` — indistinguishable from an absent key — so the slice silently falls back to `initialState` and the next mutation persists that empty state over the user's only copy of the data.
      _Fix:_ Make the failure distinguishable from absence: give `TSliceLifecycle` a third creator (or a `{ failed: true }` payload flag) that the load effect emits on `catchError`, have `providePersistedContext` gate `createSaveSliceEffect` off until a *successful* `loaded` has been seen for that key, and raise `NotificationsActions.toast` on the failure path so a read error is visible rather than silent. At minimum, correct the `versioned.ts:57` comment so it stops promising a guarantee the code does not provide. _(medium)_
- [x] **CR-005** **DONE** (0f1897b) — validated: `TItemListSortType` is `'name' | string` (open) while the switch was closed, with `default: return 0`. The comparator now infers from the value (number / date-like string / other string / absent), so the three domain names leave the kernel and an unknown key falls back to the name instead of silently not sorting. `shared` · `@shared/util/list/list.selector.ts:75` — The "domain-blind" shared comparator hardcodes three domain field names (`bestBefore`, `dueAt`, `prio`) and silently returns 0 for any other sort key, so a new list domain's sort is a no-op with no compile or lint error.
      _Fix:_ Move the comparator selection out of the kernel: have `IListPageFacade` (or the list reducer's config) supply an optional `comparators: Record<string, (a,b,dir)=>number>` that the domain owns, keep the generic `compareByName`/`compareByOptionalDate`/`compareByOptionalNumber` primitives in `@shared` as the building blocks, and make the unknown-key case loud (fall back to `compareByName` rather than `0`). _(medium)_
- [x] **CR-006** **DONE** (a0e656c) — validated: all 13 entries hardcoded `status: 'online'` and the only `.status` readers were specs. `status()` now resolves capability → read-model → declared literal; the literal survives only on the three source-less tiles. `commlink` · `commlink/data/reducer/dashboard.reducer.ts:19` — The `standby → online` tile lifecycle that the reducer, the model types, the effects comments and architecture.md §6 all describe is written into state but read by no consumer — every deck tile hardcodes `status: 'online'` instead.
      _Fix:_ Pick one: either wire it up — have `CommlinkPage.status(p)` return `this.#telemetry().bySource[p.source]?.status ?? 'standby'` for telemetry-backed programs and drop the per-entry `status: 'online'` literals — or delete `status` from `IDashboardTelemetry`, drop both `as const` stamps from the reducer, and remove the four comment blocks and the architecture.md §6 paragraph that describe it. Wiring it up is the better option: the read-model already carries exactly the information the tile is pretending to show. _(small)_
- [x] **CR-007** **DONE** (68f37c4) — validated: repo-wide grep found the type, the writer and its spec, no reader. Deleted rather than honoured: the flags default to `false` while notifications are on, so reading them would have silently muted rows. `tracking` · `tracking/util/tracking-notifications.utils.ts:78` — The per-item notification preferences (`onStart`/`onStop`/`onProcess`) are write-only — nothing in the domain ever reads them, so the three toggles in the edit dialog do nothing.
      _Fix:_ Either honour the flags in `needsStateNotification` (`item.notifications?.onStart` etc., defaulting to true so existing items keep behaving), or delete the toggles, the type, the three i18n keys and the spec until the feature is actually built. Do not leave a persisted setting with no reader. _(small)_
- [x] **CR-008** **DONE** (68f37c4) — validated: `StatsPage` injects no `ActivatedRoute`, `loaded` forces `sessionsViewId: 'today'`, and the only link was `/data/daily`. Route collapsed to `/data`; the page already has its own `ion-select` switcher. `tracking` · `tracking/routes/tracking.routes.ts:24` — The `/data/:listId` route parameter is read by nothing, and the reducer resets the view to `today` on every entry — so the app's only link into the stats page, `/data/daily`, always renders the *today* view.
      _Fix:_ Read the param in `StatsPage.ionViewWillEnter` and dispatch `changeDataView(listId)` (after the resolver's `loaded`, same timing as the §4.3 category drill), and make `selectViewMode` write back to the URL — or collapse the route to `/data` and drop the param and the reducer's forced `'today'`. _(small)_
- [x] **CR-009** **DONE** (working tree; rides on the in-flight `office-time-stats.selector` rename) — validated: `dayjs()` really was read inside the memoized projector, and `officedays` keeps its reference across midnight so navigation could not clear it. `selectTodayIsOfficeDay` deleted; the facade derives it from a refreshable `#today` signal instead. `office-time` · `office-time/data/selectors/office-time.stats.selector.ts:13` — Memoized selectors read the wall clock inside their projector, so they never recompute when only the clock moves — the "log office day" button stays stuck after midnight.
      _Fix:_ Make "now" an explicit input rather than an ambient read: keep a `today` (day-granularity) value in the slice or in a small signal source that ticks on visibility-change/midnight, and pass it into `calculateStats`/`isOfficeDay` as a selector input (`createSelector(selectOfficedays, selectToday, ...)`). That also makes the stats selectors pure and unit-testable without freezing timers. _(medium)_
- [x] **CR-010** **DONE** — selector specs landed in your parallel work; the `toBeTruthy()` effects smoke spec is replaced with behavioural ones (b7c482b). Remaining sub-points (office-time e2e, `dayjs.locale('de')` in the Vitest setup) split out as CR-010b/CR-010c below. `office-time` · `office-time/data/effects/office-time.effects.spec.ts:13` — The most arithmetic-heavy domain in the app is the only one with no selector spec, an effects spec that asserts nothing, and no e2e.
      _Fix:_ Add `office-time.stats.selector.spec.ts` using `.projector(...)` with fixed inputs for all four periods (including a DST week and a year boundary), a reducer test for `loaded` round-tripping through `serializeDates`/`deserializeIsoStrings`, and replace the smoke effects spec with marble/`firstValueFrom` tests of `loadHolidays$` success+failure and `saveOfficeTime$` serialization. Set `dayjs.locale('de')` in the Vitest setup so tests and prod agree on week start. _(medium)_
- [x] **CR-011** **DONE** (68f37c4) — validated: `TProjectedNotification` did include `status`, `stampProjected` spread it, and the producer hardcoded `'new'`. `status` is now inbox-owned; two reducer specs pin dismiss-survives-reprojection and variant-change-resets. `notifications` · `notifications/util/notifications.transforms.ts:74` — `stampProjected` spreads the producer's `status` over the existing row, so dismissing a projected notification is silently undone by the owner's next projection.
      _Fix:_ Make `status` inbox-owned, exactly like `updatedAt`: omit it from `TProjectedNotification` and have `stampProjected` keep `existing.status` while `variant` is unchanged, resetting to `'new'` when the variant changes (the same `touchedAt` predicate). Add the reducer case: dismiss → re-project unchanged variant → still `done`. _(small)_
- [x] **CR-012** **DONE** (8717cf2) — closed generically by the read registry, not per-domain. `notifications` · `notifications/data/notifications.providers.ts:48` — The save effect is not gated on hydration, and `loaded` replaces the slice wholesale, so any write in the boot window persists an empty inbox over the previous session's data.
      _Fix:_ Gate the generated save effect for `hydrate: 'boot'` contexts on the slice's own `loaded` (`skipUntil`) inside `createSaveSliceEffect`/`providePersistedContext`, so every eager persisted context gets the guard the dashboard hand-rolled. Confidence: mechanism confirmed by reading; whether the race is lost depends on IndexedDB open latency vs. the lazy chunk fetch. _(small)_
- [x] **CR-013** **DONE** (bcc2cbb) — validated: four outcomes collapsed into `undefined`, no plugin call wrapped, and neither caller had a `.catch`. Now a discriminated `TScanOutcome`; `denied`/`error` toast via `GroceryListPageFacade.reportScanFailure()`. `barcode` · `groceries/util/barcode-scanner.service.ts:35` — The entire mlkit scan path has no error handling and collapses four distinct outcomes into `undefined`, so a denied permission or a rejecting plugin call leaves the scan button permanently, silently dead.
      _Fix:_ Return a discriminated result (`{ok:true,ean} | {ok:'denied'|'cancelled'|'unsupported'|'error'}`) instead of `string|undefined`, wrap the plugin calls in try/catch, and have the grocery facade raise `NotificationsActions.toast` for denied/error. Either register the install-progress listener and re-check `isGoogleBarcodeScannerModuleAvailable()` before scanning, or drop `#ensureModule()` and let `scan()` surface its own error — the current form gives false assurance. _(small)_
- [x] **CR-014** **DONE** (2f529f0) — validated: both parsers `continue` with no counter and `IImportPlan` had no reject field. Parsers now return `{rows, rejected}`, surfaced in the preview via `cash.import.rejected`. `cash` · `cash/util/import/plan-import.ts:6` — CSV rows the parser cannot read are dropped silently and never counted, so a partial import is indistinguishable from a complete one.
      _Fix:_ Have `IBankParser.parse` return `{ rows, rejected }` (or have `planImport` take the raw row count), add `rejected: number` to `IImportPlan`, and surface it in `cash.import.summary`; consider disabling confirm when `rejected > 0` until the user acknowledges. _(small)_
- [x] **CR-015** **DONE** (2f529f0) — validated: `splitRow` was a bare `split(';')`. Now RFC4180-aware. Note the shipped samples are unquoted, so the reviewer's "parses to zero rows" claim was about real bank exports, not the fixtures. `cash` · `cash/util/import/bank-parser.ts:35` — `splitRow` splits on `;` with no quoted-field handling, so a quoted bank export parses to zero rows and reports 'no new bookings'.
      _Fix:_ Replace `splitRow` with a small quote-aware field splitter (handles `"…"`, embedded `;`, and `""` escapes) — it is ~15 lines and fixes both the header lookup and the amount parse. Add a quoted-CSV case to both parser specs. _(small)_
- [x] **CR-016** **DONE** (2f529f0) — validated: `dayjs('').format()` returns the literal string `Invalid Date`. Both edit modals now gate `canSave` on `isValid()`. `cash` · `cash/feature/transaction-edit-modal/transaction-edit-modal.component.ts:105` — `canSave` validates description and amount but not the date, so an empty date field persists `dateISO: "Invalid Date"`.
      _Fix:_ Add `dayjs(draft.date).isValid()` to `canSave` in both modals (and mirror the existing `amountInvalid` note with a date-invalid one), or refuse to persist a non-valid date in `persist`. _(trivial)_
- [x] **CR-017** **DONE** (2f529f0) — validated: `removeCategory` left `rules` untouched and `categorize` does not check the catalog. Delete now cascades; hydration prunes rules orphaned by older builds. `cash` · `cash/data/reducer/cash.reducer.ts:97` — Deleting a category does not cascade into rules, and "Regeln anwenden" then re-stamps the dead category id back onto transactions.
      _Fix:_ Cascade `removeCategory` into `rules` (drop rules whose only purpose was that category, or clear `categoryId` and treat a category-less rule as inert), and/or make `categorize` skip rules whose `categoryId` is not in the catalog. Add a reducer spec case for the rule side of the delete cascade. _(small)_
- [x] **CR-018** **DONE** (1b9ad86) — validated: `snapshotFor` captured `config` and `restoreLastDeleted` spread the whole snapshot back. `config` removed from `ITrackplaySnapshot`; spec pins that a sort change survives an undo. `trackplay` · `trackplay/data/reducer/trackplay.reducer.ts:367` — `restoreLastDeleted` replaces the entire slice with a pre-delete snapshot, so every unrelated edit made during the 8-second undo-toast window is silently discarded.
      _Fix:_ Snapshot only what the delete actually removed (the player/game/type plus its rounds and the filter tweak) and re-insert that, or make the undo a targeted inverse action per delete kind. If a whole-state snapshot is kept, at minimum exclude `config` and invalidate `lastDeleted` on any subsequent mutating action. _(medium)_
- [x] **CR-019** **DONE** (1b9ad86) — validated against `@ionic/core`: `getPresentedOverlay` filters on `overlay-hidden` (added only after the ~300ms leave) while `dismiss()` on a leaving overlay resolves false, so the loop could spin on microtasks. Effect now dismisses its own toast instance. `trackplay` · `trackplay/data/effects/trackplay.effects.ts:39` — The `while (await getTop()) await dismiss()` loop can never terminate when the top toast is already mid-dismiss, starving the microtask queue and freezing the tab.
      _Fix:_ Hold the created toast in a field and dismiss that instance (`await this.#current?.dismiss()`), or bound the loop (`for (let i = 0; i < 3; i++)`), and scope the dismissal to trackplay's own toast rather than every presented toast in the app — as written it also tears down `NotificationsToastEffects` messages from other domains. _(small)_
- [x] **CR-020** **DONE** (a8718ed) — validated: `bootstrap()` reads nothing, `runMigrations` reads each doc's own envelope, settings declares no ladder — so nothing read the seeded doc. `seed$` deleted; `npc-settings` now has exactly one writer. `settings` · `settings/data/effects/settings.effects.ts:28` — `seed$` treats a failed storage read as a fresh install and overwrites the user's persisted theme with the hardcoded default, violating the load pipeline's documented "must never wipe" contract.
      _Fix:_ Delete `seed$`. It buys nothing: nothing reads the seeded doc's `v` — `DatabaseService.bootstrap()` deliberately reads nothing (`database.service.ts:26-31`), `runMigrations` reads each doc's own envelope (`versioned.ts:66`), and settings declares no ladder — and a fresh install with the key absent already resolves to `initialSettings` via `loaded(null)`. Removing it also drops the domain's direct `DatabaseService`/`wrapVersioned`/`APP_VERSION` imports, leaving exactly one writer for `npc-settings`. _(trivial)_
- [x] **CR-021** **DONE** (a8718ed) — `settings.effects.spec.ts` added: theme→DOM on both `loaded` and `setTheme`, splash lifts on hydration only, and no write on a null load. `settings` · `settings/data/effects/settings.effects.ts:19` — `SettingsEffects` is the only hand-written effects class in the app with no spec, and it owns the boot-splash lift, the theme→DOM bridge and the seed write.
      _Fix:_ Add `settings.effects.spec.ts` with `provideMockActions` + `provideMockStore`: assert `revealSplash$` calls a `SplashService` spy on `loaded`, that `applyTheme$` calls `ThemeService.apply` with the hydrated theme on both `loaded` and `setTheme`, and (if `seed$` survives finding #1) that it does not write when `loaded` carries state. Add one Playwright spec: open `/#/settings`, click the boomer segment host (not its inner button — the repo's own Ionic trap), assert `html[data-theme=boomer]`, reload, assert it persisted. _(small)_

## MEDIUM

- [ ] **CR-022** `repo` · `office-time/data/selectors/office-time.selector.ts:1` — The office-time selector layer executes zero projector functions and zero branches under test — statements only register because the module was imported.
      _Fix:_ Add `office-time.selector.spec.ts` exercising each projector via `.projector(...)` (the repo's documented pattern, already used in 94 other selectors), and add one `e2e/office-time/first-paint.e2e.ts` mirroring the existing per-domain first-paint specs. _(small)_
- [x] **CR-023** **DONE** (98b6419) — cash-account import wiring + cash-rules applyRules + both modals now spec'd. `repo` · `commlink/feature/commlink-page/commlink.page.ts:98` — commlink.page.ts — the largest class in the app (209 LOC) and the app's home screen — has no spec and is absent from the coverage report, while the commlink domain reports 100% coverage.
      _Fix:_ Add commlink.page.spec.ts covering `status(p)` for all five LanguageModel availability values and asserting `onlineCount` recomputes when the capability signal flips. _(small)_
- [x] **CR-024** **OPEN — do not trust the new spec.** A replacement `tracking.effects.spec.ts` (trackTime$ + CSV export) was written and is in the working tree **unstaged**: it is order-dependent flaky (`vi.mock('@capacitor/share')` under `isolate: false`), failing roughly one run in two. HEAD keeps the old smoke spec so the suite stays deterministic. Fix the module mock or drop the file. `repo` · `settings/data/effects/settings.effects.ts:48` — The two-theme feature — a headline capability of this app — has neither a unit test nor an e2e test anywhere.
      _Fix:_ Add settings.effects.spec.ts asserting the ordering (data-theme set before reveal) with a mocked ThemeService/SplashService, plus one e2e that switches theme on /#/settings and asserts `html[data-theme]` flips and survives a reload. _(small)_
- [ ] **CR-025** `repo` · `trackplay/data/reducer/trackplay.reducer.ts:1` — trackplay is the lowest-covered domain at 63.0% statements, and its 388-line reducer — the largest source file in the repo — leaves 16 of 42 handlers and 30 of 62 branches unexecuted.
      _Fix:_ Extend trackplay.reducer.spec.ts to cover the 16 unexecuted handlers; v8's HTML report at coverage/np-commlink/trackplay/data/reducer/ names the exact uncovered line ranges (327-332, 350-361, …). _(medium)_
- [x] **CR-026** **DONE** (8717cf2) — rejection handled; surfacing a *write* failure to the user is still open, tracked as CR-026b. `shared` · `@shared/data/effects/persisted-slice.effects.factory.ts:96` — Every persisted write is fire-and-forget with no error handling — a rejected `database.save` becomes an unhandled promise rejection and the user is never told the write did not land.
      _Fix:_ `switchMap`/`mergeMap` the save into the stream with a `catchError` that dispatches `NotificationsActions.toast({ key: 'toast.save-failed', color: 'danger' })` (the contract already exists and is reachable from `@shared/data`), and add a spec case that mocks a rejecting `save`. _(small)_
- [ ] **CR-027** `shared` · `@shared/model/item-list.types.ts:16` — `IItemList.title` is a required field that every one of the six list slices must supply and that gets persisted to disk on every save, but nothing ever reads it.
      _Fix:_ Delete `title` from `IItemList` (and `id?`, if the same audit shows nothing reads it — `stateByListId` switches on the route param, not on `state.id`). Removing a persisted field is read-safe with no migration: old docs just carry an ignored key. _(small)_
- [ ] **CR-028** `shared` · `@shared/feature/list-page/list-page.component.ts:122` — The rule "in categories mode the add affordance creates a category, not an item" is implemented in three different layers, and the shell's comment claims it was consolidated into one.
      _Fix:_ Move the branch entirely into `ListPageComponent.addItemFromSearch()` (mirroring `showCreateDialog`), delete the check from `TasksListPageFacade` and from `GroceryListEffects.routeAddItemFromSearch$`, and fix the comment to describe what is actually true. _(small)_
- [ ] **CR-029** `shared` · `@shared/util/list/list.utils.ts:62` — `updateListItem` falls back to matching by name when the DTO's id is absent from the list, and then spreads the DTO's `id` over the matched row — silently rewriting a different item's identity.
      _Fix:_ In `updateListItem`, match by id only (the name fallback exists for `addOrUpdateItem`, which is a different call site with different intent), or strip `id` from the spread. Add the currently-avoided case to `list.utils.spec.ts` as a pinned no-op. _(small)_
- [ ] **CR-030** `shared` · `@shared/util/theme.service.ts:20` — The theme/splash/settings cluster lives in `@shared` but has exactly one consumer domain, which is the case the repo's own "shared-ness is who imports it" rule says should move home.
      _Fix:_ Move `theme.service.ts`, `splash.service.ts` and `settings.types.ts` (+ `TTheme`) into `settings/util` and `settings/model`. The only blocker is `mockSettings`/`TMockKernelState` in the shared test kit — resolve it the way `commlink` already did: a `settings/testing` fixture seeded through `TMockState`'s `Record<string, unknown>` half, which is precisely the hatch `test-data.ts:21-24` documents for exactly this reason. _(small)_
- [ ] **CR-031** `shared` · `@shared/util/list/list.utils.ts:28` — Eleven of the thirteen shared list-reducer helpers — including the category rename-merge and delete-cascade used by six slices — are covered only from a grocery domain spec, against grocery fixtures.
      _Fix:_ Move the shared-helper `describe` blocks out of `grocery-list.utils.spec.ts` into `@shared/util/list/list.utils.spec.ts`, against a neutral `IListState<IBaseItem>` probe (the file already has a `listWith()` factory). Leave only the genuinely grocery-specific helpers (`stateByListId`, `listIdByPrefix`, `addListItemOrIncreaseQuantity`, …) in the domain spec. _(small)_
- [ ] **CR-032** `shared` · `@shared/ui/categories/categories-dialog/categories-dialog.component.ts:163` — The most logic-dense shared UI component — the category picker used by grocery, tasks and cash — has no spec at all, including the dedupe branch that exists to fix a specific bug.
      _Fix:_ Add a class-level spec (`TestBed.createComponent(...).componentInstance`, no `detectChanges` per the repo's convention) covering: existing-name → no `addNew` emission and the existing id selected; new name → `addNew` with a minted id; single mode → `confirmed` on row tap; `isOpen` toggle → selection reset discards uncommitted toggles. _(small)_
- [ ] **CR-033** `commlink` · `commlink/data/effects/dashboard.effects.ts:73` — The `skipUntil(hydrate)` gate only protects the boot race; a lazy context's registration-time zero report still overwrites that source's persisted summary, and on a failed slice load the overwrite is permanent.
      _Fix:_ Make the reporter, not the persister, aware of hydration: have `createTelemetrySliceEffect` take the context's `loaded` creator and `skipUntil` it (the same trick `persistSummary$` already uses), so a context never reports pre-hydration state at all. That fixes the write *and* stops a lazy tile from briefly flashing an incorrect live value on the deck. Failing that, gate `persistSummary$` per-source rather than globally. _(small)_
- [ ] **CR-034** `commlink` · `commlink/data/reducer/dashboard.reducer.ts:22` — For the eager `notifications` reporter, hydrate's gap-fill discards the persisted summary in favour of an initialState zero — so the badge's documented cold-launch guarantee does not hold, and the comment justifying the design is false.
      _Fix:_ Same root cause as the previous finding — defer the telemetry reporter until its slice's `loaded` fires. Then hydrate's "already present ⇒ reported live" premise becomes true by construction and the comment becomes accurate. Fix the comment either way; as written it asserts something the boot ordering contradicts. _(small)_
- [x] **CR-035** **DONE** (61dd01e) — `loadPrefixed` now selects keys via `keys()` and reads only the matching ones, so the big slice docs are never deserialized. `commlink` · `commlink/data/effects/dashboard.effects.ts:40` — `loadPrefixed('summary-')` on the eager boot path reads and deserializes the *entire* datastore, not just the summary docs.
      _Fix:_ Collapse the key family into one `npc-summaries` doc holding `Record<source, IDashboardSummary>` — the write is still tiny (10 sources × a few numbers), the read becomes a single `get`, and commlink still owns its own keyspace. That also retires `loadPrefixed` and its single caller from the shared port. If the per-source keys must stay, at minimum stop claiming in the docs that boot reads only the summaries. _(medium)_
- [ ] **CR-036** `commlink` · `commlink/feature/commlink-page/commlink.page.ts:234` — `CommlinkPage` has no spec, so the capability-gated status mapping — the branch that only matters on the APK target — has zero coverage in either test layer.
      _Fix:_ Add `commlink.page.spec.ts` in the repo's established shape (`TestBed.createComponent(...).componentInstance` + `provideMockStore` + `provideZonelessChangeDetection`, no `detectChanges`): stub `LanguageModelService.availability` as a writable signal and assert all five rows of the mapping plus the resulting `onlineCount`, and seed `mockDashboardState` to cover `badge()` returning null / a number / NaN-from-string. _(small)_
- [ ] **CR-037** `commlink` · `commlink/data/effects/dashboard.effects.ts:82` — Summary docs are written raw, bypassing the `{v, data}` versioned envelope every other persisted doc in the app carries — and read back with no `runMigrations` and no shape validation.
      _Fix:_ Either put the summary docs on the envelope (`wrapVersioned` on save, `runMigrations` per doc on load — cheap, and consistent with the stated invariant), or record explicitly in `commlink/model/dashboard.types.ts` that this keyspace is deliberately unversioned and disposable, and have `hydrate` drop entries without a `source`. Collapsing to a single `npc-summaries` doc (previous finding) makes the envelope trivial to apply. _(small)_
- [ ] **CR-038** `tracking` · `tracking/data/effects/tracking-notifications.effects.ts:111` — A dismissed tracking notification resurrects into the inbox's "new" section on the next projection, because every projected row hardcodes `status: 'new'`.
      _Fix:_ Preserve the existing status in `stampProjected` when the variant is unchanged (`status: existing?.origin?.variant === variant ? existing.status : content.status`) — the same rule `touchedAt` already applies to `updatedAt`. Tracking cannot fix this on its side; it cannot read the inbox by design. _(small)_
- [x] **CR-039** **DONE** (dc162eb) `tracking` · `tracking/data/effects/tracking-notifications.effects.ts:41` — `reconcileState$` does not re-project on `updateItem`, so renaming a tracked activity leaves a stale name in its inbox row until the next toggle.
      _Fix:_ Add `TrackingActions.updateItem` to the `ofType` list. `#targetIdOf` already handles the `{ item }` payload shape, so no other change is needed. _(trivial)_
- [x] **CR-040** **DONE** (dc162eb) — renamed only (`stopOtherItem`/`pauseTracking`); harmonizing the two paths onto one resumable state is deferred, per the finding's own "or, better" alternative. `tracking` · `tracking/data/reducer/tracking.reducer.ts:28` — Two reducer helpers are named for the opposite state they actually write: `pauseOtherItem` writes `state: 'stopped'` and `stopTracking` writes `state: 'paused'`.
      _Fix:_ Rename to what they write (`stopOtherItem` / `pauseTracking`), or — better — make the two paths agree on `'paused'` for an interrupted-but-resumable item so the inbox offers the resume CTA in both cases. This is exactly the class commit 7f776bb went after. _(trivial)_
- [x] **CR-041** **DONE** (dc162eb) `tracking` · `tracking/ui/tracking-item/tracking-item.component.html:24` — The item status badge renders hardcoded English while the three matching i18n keys are marked, translated in de/en, and unused.
      _Fix:_ Bind `'tracking.item.state.' + trackingItem.state | translate` and drop the three standalone `marker()` calls (the dynamic key needs them kept, so keep exactly those three). While there, `list-header.tracking.search` in de/en is dead too. _(trivial)_
- [ ] **CR-042** `tracking` · `tracking/data/reducer/tracking.reducer.ts:188` — Two reducer arms are non-deterministic — `saveAndReset` mints `uuidv4()` ids and `seedDemoSessions` calls `Math.random()`, `uuidv4()` and `dayjs()` — in a domain that otherwise threads `now` through the action payload precisely to keep the reducer pure.
      _Fix:_ Move id minting and `now` into the action creators (`saveAndResetTracking: (now, ids)` or archive via an effect that dispatches a fully-formed payload), matching `toggleTrackingItem`. At minimum do it for `saveAndReset`, which is a production path. _(medium)_
- [ ] **CR-043** `tracking` · `tracking/data/index.ts:16` — The `tracking/data` public barrel is roughly four times wider than production needs it to be — `TrackingActions`, `DailySeries` and all six exported selectors are imported from `../../data` only by `*.spec.ts`.
      _Fix:_ Have the component specs seed state through the facade (or override `LIST_FACADE`/inject `TrackingListPageFacade` with a stubbed store) rather than overriding module-level selectors, then shrink the barrel to `TrackingListPageFacade` + `trackingContext`. If that is judged too costly, at least correct the docstring to say the extra exports exist for tests. _(medium)_
- [x] **CR-044** **DONE** (0a01f7b, prior to this sweep) — verified: `TrackingActions.enterPage` no longer exists in the actions file; the doc had just never been updated. `tracking` · `tracking/data/actions/tracking.actions.ts:21` — `TrackingActions.enterPage` is dead — dispatched from both pages via the facade, handled by no reducer arm and no effect — and a spec asserts the dead dispatch, which locks it in.
      _Fix:_ Delete the action, the facade method, the two call sites and the spec case. (`tasks` has the same dead `enterPage` via the shared factory — worth checking in the same pass.) _(trivial)_
- [ ] **CR-045** `tracking` · `tracking/data/tracking-list-page.facade.ts:52` — `TrackingListPageFacade` is five facades in one class; its name advertises only the `IListPageFacade` binding, so nothing about the stats page, chart, daily-sessions panel or CSV export is discoverable from it.
      _Fix:_ Split into `TrackingFacade` (domain: timer commands, sessions, CSV, stats view) and a thin `TrackingListPageFacade implements IListPageFacade` that delegates — mirroring the doc's own facade table where the list binding and the domain facade are distinct concepts. _(medium)_
- [ ] **CR-046** `tracking` · `tracking/data/selectors/tracking.selector.ts:45` — An aggregated stats row borrows the id of the last session merged into it, so deleting a merged row deletes exactly one of its underlying sessions.
      _Fix:_ Give `IDataItem` an explicit `sessionIds: string[]` (or make its `id` the composite group key) and have `removeDataItem` filter on membership. That also makes `track item.id` in `stats.page.html:39` correct by construction. _(small)_
- [ ] **CR-047** `tracking` · `tracking/data/reducer/tracking.reducer.ts:174` — The `data` → `sessions` storage-key hop is hardcoded in the reducer's `loaded` arm, bypassing the migration ladder that `providePersistedContext` exists to run — and the comment justifying it is inaccurate.
      _Fix:_ Bump `APP_VERSION` to 2, add a one-step ladder on `trackingContext` that renames `data` → `sessions`, and delete `legacySessions` plus its two reducer specs (moving them onto the migration step). _(small)_
- [ ] **CR-048** `tracking` · `tracking/data/effects/tracking.effects.spec.ts:12` — `TrackingEffects` — the domain's live timer and its CSV export — has only a 25-line "is created" smoke test; neither `trackTime$` nor `#buildCsv` is exercised.
      _Fix:_ Add a marble/fakeAsync spec for `trackTime$` covering: ticks while an item runs, completes when `selectRunningTrackingItem` goes undefined, restarts on `loaded`. Add one `#buildCsv` case per view id (extract the private into a pure `buildCsv(data, viewId, t)` in `util/` if that reads better). _(small)_
- [ ] **CR-049** `office-time` · `office-time/feature/office-time-page/office-time.page.ts:70` — The card-visibility rule is written twice — a 12-branch switch in the page class and the identical flag check repeated inside 10 template `@case` blocks.
      _Fix:_ Replace the switch and the `@if`s with one `Record<DashboardItemType, keyof DashboardSettings | null>` in `model/office-time.types.ts` next to `DASHBOARD_ITEMS` (the two vocabularies then live side by side and TypeScript enforces exhaustiveness), filter with it in `visibleDashboardItems`, and delete every `@if (dashboardSettings().showX)` from the template. _(small)_
- [ ] **CR-050** `office-time` · `office-time/data/index.ts:36` — The data barrel's docstring claims a facade-only surface, but it re-exports 12 selectors with no production consumer plus 5 pure date utils that belong to `util/`.
      _Fix:_ Drop the util re-exports and import `../../util/office-time.utils` directly from the three smart-ui components; drop the selector exports (the one spec that needs `selectTodayIsOfficeDay` can seed state through `mockOfficeTimeState` instead of `overrideSelector`), leaving the barrel at `OfficeTimeActions` + `OfficeTimeFacade` + `officeTimeContext` — which is what the docstring already promises. _(small)_
- [ ] **CR-051** `office-time` · `office-time/data/actions/office-time.actions.ts:28` — Six dead symbols across the domain, including a duplicate action with its own reducer branch and save trigger.
      _Fix:_ Delete `addOfficeday` (and its reducer/`saveOn$` entries), `saveOfficeTimeSuccess` (make `saveOfficeTime$` `dispatch: false` like the shared factory), `TSettings.targetDate` and `DashFreedaysEditComponent.today`; collapse `initOfficeTime` into `loadHolidays` on the facade. Either use `mockOfficeTimeState` in the new reducer/selector specs or delete the fixture file. _(small)_
- [ ] **CR-052** `office-time` · `office-time/ui/dash-date/dash-date.component.ts:32` — The date card snapshots the date once at construction while running a 1 Hz timer forever, so it shows the wrong day after midnight and burns change detection while off-screen.
      _Fix:_ Derive the whole card from one ticking source (`toSignal(interval(1000))` → `computed(() => …)`), so the date rolls over with the clock, and gate the interval on document visibility (or align the tick to the second and pause on `visibilitychange`, mirroring what `refreshOnYearRollover$` already does for holidays). _(small)_
- [x] **CR-053** **DONE** (7271082) — validated: nine stats, seven `periodDaysMatching` calls, each materializing the whole window via `allDaysBetween`, then O(days x entries) `isSame` scans. Now one pass with `Set<YYYY-MM-DD>` indexes; the six single-purpose period helpers and the tautological `daysInRangeMatching` boundary re-filter are gone (no other callers). `office-time` · `office-time/util/office-time.utils.ts:95` — `calculateStats` materializes the whole period seven times per call across four periods, with a redundant clone and a tautological re-filter in the inner loop.
      _Fix:_ Build the period's day list once per `calculateStats` call and derive all seven counts from a single pass; drop the `.clone()` and the tautological boundary filter in `daysInRangeMatching`; pre-index `officedays`/`freedays`/`holidays` into `Set<string>` of `YYYY-MM-DD` (the domain already has `dayjsToString`) so membership is O(1) instead of an `isSame` scan. _(medium)_
- [x] **CR-054** **DONE** (7cb06c6) `office-time` · `office-time/util/office-time.utils.ts:236` — `validateFreedays` is the one date comparison in the domain that omits the `'day'` unit, so it silently degrades to an exact-timestamp match.
      _Fix:_ Change to `holiday.isSame(day, 'day')`, and add a spec case where the holiday carries a time component so the unit is pinned by a test rather than by luck. _(trivial)_
- [ ] **CR-055** `notifications` · `notifications/feature/notifications-page/notifications.page.ts:123` — The "domain-blind" inbox hardcodes tracking's command vocabulary to label CTAs, and renders an empty, unlabeled button for any command it does not recognise.
      _Fix:_ Move the label onto the port: add `labelKey: TMarker` to `TNotificationAction` (producers already build titles from marker keys) and delete the switch; render the button only when a label exists. That also removes the icon-registration coupling argument. _(small)_
- [x] **CR-056** **DONE** (93edf13) — also moved the two keys to `notifications.debug.*`, closing CR-150 in the same pass. `notifications` · `notifications/feature/notifications-page/notifications.page.html:139` — Debug affordances ship to production users on the notifications page, ungated, despite the app having an `isDevMode()` precedent.
      _Fix:_ Wrap the block in `@if (isDev)` like tracking does, and register `NotificationsDebugEffects` only under `isDevMode()`; rename the two keys to `notifications.debug.*` while touching them. _(trivial)_
- [x] **CR-057** **PARTLY DONE** (256f1ed) — the hardcoded German copy is now i18n via `translate.get` (not `instant`, which would have scheduled the raw key from the boot path). The unconditional permission request stays: making it opt-in needs a settings surface to opt in from, which is a product decision. `notifications` · `notifications/util/notification.service.ts:29` — The OS office reminder requests notification permission at every cold boot, is unconditional and unconfigurable, and its user-facing text bypasses i18n entirely.
      _Fix:_ Request permission in context (from the notifications page or an office-time setting) rather than at boot; put the two strings behind `marker(...)` keys resolved via `TranslateService`; add an opt-out flag; and narrow the catch so only the "not implemented" web path is silent. _(medium)_
- [x] **CR-058** **DONE** (93edf13) `notifications` · `notifications/data/actions/notifications.actions.ts:10` — The action group's doc comment describes a source-prefixed save trigger, but the context uses a hand-maintained explicit `on:` list — and adopting the described mechanism would make every transient `toast` rewrite the persisted inbox.
      _Fix:_ Fix the comment to describe the explicit list and say why (`toast` must be excluded), or keep `sources` and make `toast` a separate source. Either way this is the answer to "does `toast` belong on this group": cohesion-wise yes, but the persistence trigger has to say so out loud. _(trivial)_
- [x] **CR-059** **DONE** (93edf13) `notifications` · `notifications/data/effects/notifications-toast.effects.ts:47` — `translate.instant` on the boot-path toast renders the raw i18n key when translations have not loaded yet — which is exactly the case this effect is registered eagerly for.
      _Fix:_ Use `firstValueFrom(this.#translate.get(key, params))` (or `stream`) in `#present` — `get` waits for a pending language load — and keep the existing `concatMap` so ordering is preserved. _(trivial)_
- [x] **CR-060** **DONE** (2d1e38a) `barcode` · `barcode/util/barcode.utils.ts:33` — Optional chaining on the 2D canvas context turns a rotation failure into a silent overwrite of the user's badge with a blank image, which is then persisted.
      _Fix:_ Replace the three `context?.` calls with an early `if (!context) return;` so a missing context is a no-op rotation, and drop the `rotated !== state.dataUrl` heuristic in the effect (it cannot distinguish a blank canvas from a real rotation). _(trivial)_
- [ ] **CR-061** `barcode` · `barcode/data/effects/barcode.effects.ts:26` — `switchMap` combined with `withLatestFrom` silently discards a second rotate tap, so double-tapping rotate turns the badge 90° instead of 180°.
      _Fix:_ Use `concatMap` and move the state read inside it (`concatMap(() => this.#store.select(selectBarcodeState).pipe(take(1), switchMap(...)))`) so each queued tap rotates the *result* of the previous one; `withLatestFrom` outside the flattening operator would still hand the queued tap stale state. Alternatively `exhaustMap` plus a disabled/busy state on the button. _(small)_
- [ ] **CR-062** `barcode` · `barcode/smart-ui/barcode-input/barcode-input.component.ts:20` — The upload path validates nothing and its only error handler covers the failure that essentially never happens, so an undecodable image is stored, persisted, and shown as a broken picture with no message.
      _Fix:_ Probe decodability before dispatching (`createImageBitmap(file)` or an `Image` whose `onerror` routes to `reportUploadFailure()`), and reuse the existing toast for the unsupported-format case rather than only for reader errors. _(small)_
- [ ] **CR-063** `barcode` · `barcode/util/barcode.utils.ts:37` — `canvas.toDataURL('image/*')` is not a valid MIME type, so every rotation silently re-encodes the badge as PNG and can multiply the size of the persisted document several-fold.
      _Fix:_ Pass the source MIME through (parse it off the incoming data URL, default `image/png`) or explicitly choose `image/jpeg` with a quality argument for photo badges. Delete the unreachable non-`data:` branch on line 12-14. Consider downscaling to a sane max dimension before storing — a badge is displayed at screen size, not print size. _(small)_
- [ ] **CR-064** `barcode` · `barcode/feature/barcode-page/barcode.page.html:13` — The sealed `barcode` domain still speaks office-time's i18n vocabulary — every user-visible string is keyed under `officetime.page.settings.barcode.*`, a settings page that no longer exists.
      _Fix:_ Move the four keys to `barcode.*` in `de.json`/`en.json` (and update `page-title.barcode`'s neighbours accordingly), then widen the eslint domain-vocabulary gate so a file under `src/app/<domain>/` cannot name another domain's prefix — the rule already has the pattern, it just needs the scope. _(small)_
- [x] **CR-065** **DONE** (98b6419) — `barcode.utils.spec.ts` + `barcode.effects.spec.ts` added, including the documented emit-nothing-on-unchanged-image rule. `barcode` · `barcode/util/barcode.utils.ts:5` — The only non-trivial logic in the domain — the rotation geometry and the effect's documented suppression rule — has no unit test, and barcode is the only lazy persisted context with no e2e reload guard.
      _Fix:_ Unit-test `rotateBase64` for the dimension swap and the `undefined`/failed-load cases, and marble-test the effect for the "emit nothing when rotation produced no new image" rule. Add `e2e/barcode/persistence.e2e.ts` using Playwright's `setInputFiles` against `#image-input` (it works on hidden inputs) → assert the `<img>`, cold-reload, assert it survived. _(medium)_
- [ ] **CR-066** `tasks` · `tasks/routes/tasks.routes.ts:31` — The `:listId` route parameter is never read by anything in the tasks domain, so the URL advertises a multi-list domain that does not exist and any string works as a list id.
      _Fix:_ Collapse the leaf to a static path (`path: ''` under the `/tasks` subtree, or `path: 'list'`) and drop the four hardcoded `_tasks` URLs; keep `TASKS_LIST_ID` purely as the `ItemDialogService` handshake token it is documented to be. _(small)_
- [x] **CR-067** **DONE** (0a01f7b, prior to this sweep) — verified: no `enterPage` on `TasksListPageFacade`; the doc just never caught up. `tasks` · `tasks/data/tasks-list-page.facade.ts:112` — `TasksActions.enterPage` has no reducer case and no effect listening for it — its only observable effect is a full-document IndexedDB write on every page entry.
      _Fix:_ Drop `enterPage()` from `TasksListPageFacade` and the `ionViewWillEnter` call in `tasks.page.ts:42`; if the action stays in the shared factory, that is fine — it just should not be dispatched by a domain that ignores it. Delete the `tasks.page.spec.ts:31` test that pins it. _(trivial)_
- [x] **CR-068** **DONE** (8099f65) `tasks` · `tasks/data/tasks.providers.ts:27` — `save: { sources: ['[Tasks]'] }` persists the whole slice on pure view-state actions (search, filter, mode, sort-toggle, page entry, duplicate-add failure), not just on mutations.
      _Fix:_ Switch to the explicit `on: [TasksActions.addItem, removeItem, updateItem, updateSort, addCategory, removeCategory, updateCategory]` form tracking already uses. _(trivial)_
- [ ] **CR-069** `tasks` · `tasks/data/tasks-list-page.facade.ts:57` — The "in categories mode, add-from-search creates a category instead of an item" decision is implemented a third time here, after the shared shell already claimed ownership of exactly that decision.
      _Fix:_ Move the mode branch into `ListPageComponent.addItemFromSearch()` (call `facade.addCategoryFromSearch()` when `state.mode === 'categories'`), so `IListPageFacade.addItemFromSearch` becomes the unconditional item command its name promises; grocery's `routeAddItemFromSearch$` then collapses too. _(small)_
- [ ] **CR-070** `tasks` · `tasks/data/tasks-list-page.facade.ts:38` — Neither `TasksListPageFacade` nor `TasksCategoriesPageFacade` has a spec, although they hold every branching decision the domain makes — and the sibling domain does spec its facade.
      _Fix:_ Add `tasks-list-page.facade.spec.ts` and `tasks-categories-page.facade.spec.ts` modelled on the tracking one (`provideMockStore` + `overrideSelector(selectTasksState, …)` + `afterEach(resetSelectors)`), covering the two mode branches, the dialog seed, and `drillTo`. _(small)_
- [ ] **CR-071** `tasks` · `tasks/feature/tasks-page/tasks.page.ts:60` — `getItemStatusColor` is an untested domain rule (a due-date urgency threshold) living in a component, while the domain's `util/` folder next door holds only dead code.
      _Fix:_ Move it to `tasks/util/task.utils.ts` as `dueStatusColor(item, now = dayjs())` with an injectable `now` and spec the three boundaries; the page becomes a one-line delegation. _(small)_
- [ ] **CR-072** `tasks` · `@shared/feature/edit-item-dialog/base-edit-item-dialog.ts:51` — The item-dialog open-command is a root-singleton signal that nothing clears on navigation, so leaving /tasks with the edit dialog open leaves the abandoned draft armed to re-present on return.
      _Fix:_ Clear the command on navigation — either `inject(Router).events.pipe(filter(NavigationStart))` → `close()` inside `ItemDialogService`, or a `DestroyRef` teardown in `BaseEditItemDialog` that closes its own request. _(small)_
- [x] **CR-073** **DONE** (abb10e6) `cash` · `cash/feature/transaction-edit-modal/transaction-edit-modal.component.ts:148` — Deliberately clearing a category is not recorded as a manual override, so the next rule run re-assigns the category the user just removed.
      _Fix:_ Set `categoryManual: true` whenever the dialog's category field was touched (or unconditionally on save, matching the comment's intent), so 'no category, by choice' is a shielded state; keep `undefined` only for transactions the user never opened. _(trivial)_
- [ ] **CR-074** `cash` · `cash/feature/cash-rules-page/cash-rules.page.ts:166` — "Apply rules" dispatches one action per changed transaction, re-serializing and rewriting the entire ledger to IndexedDB N times.
      _Fix:_ Add a bulk `recategorizeTransactions(changes: ICashRecategorization[])` action + reducer case (one map over transactions) so the whole run is one dispatch and one persist, mirroring `importTransactions`. _(small)_
- [x] **CR-075** **DONE** (98b6419) — `cash-account.page.spec.ts` covers the import wiring, `cash-rules.page.spec.ts` covers apply-rules, and both modals are spec'd. `cash` · `cash/feature/cash-account-page/cash-account.page.ts:153` — The three money-critical compositions — import wiring, apply-rules, reconcile-confirm — have neither a component spec nor an e2e; only their pure halves are tested.
      _Fix:_ Add a `cash-account.page.spec.ts` driving `importCsv` with a synthetic `File` (fixture CSV text) and asserting the dispatched `importTransactions` payload, plus a `cash-rules.page.spec.ts` for `applyRules` (change set + toast count). One e2e that imports a fixture CSV end to end would cover the rest. _(medium)_
- [x] **CR-076** **DONE** (abb10e6) `cash` · `cash/data/reducer/cash.reducer.ts:138` — `reorderRules` rebuilds the rule list from the supplied id array, so any id omitted from the payload silently deletes that rule.
      _Fix:_ Reorder in place — renumber the ids present and append the untouched rules after them — or rename the action to `setRuleOrder(ids)` and assert totality. Add a spec case for a partial id list. _(trivial)_
- [ ] **CR-077** `trackplay` · `trackplay/data/trackplay.providers.ts:25` — The transient undo snapshot is persisted: `save` writes `selectTrackplayState` verbatim, so `npc-trackplay` carries a full duplicate copy of the whole slice from the first delete onward.
      _Fix:_ Keep `lastDeleted` out of the persisted shape — either strip it in the save selector (a `selectPersistedTrackplay` projection) or move the single-slot undo out of the store entirely (it is transient UI state, like the dialog open-command already moved to `ItemDialogService`). _(small)_
- [ ] **CR-078** `trackplay` · `trackplay/data/actions/trackplay.actions.ts:20` — Four of the five `enter*Page` actions are no-ops with no reducer case and no effect, yet each one triggers a full slice write to IndexedDB on every page entry.
      _Fix:_ Delete the four no-op actions, their facade methods, their `ionViewWillEnter` hooks and their specs; keep `enterGamePage`, which earns its place by guaranteeing the trailing blank round. _(small)_
- [x] **CR-079** **DONE** (54e57d7) `trackplay` · `trackplay/data/index.ts:17` — The data barrel re-exports 16 selectors that nothing outside `trackplay/data` imports, and its docblock claims they are 'the read selectors they actually query'.
      _Fix:_ Trim the barrel to `TrackplayFacade` + `trackplayContext` (+ `TrackplayActions` only if the spec-import decision stays), and correct the docblock. This is the last domain still exporting its whole selector surface. _(trivial)_
- [ ] **CR-080** `trackplay` · `trackplay/feature/game-edit-modal/game-edit-modal.component.ts:166` — The create path reverse-engineers the new game's id by diffing store-map keys before/after dispatch, then patches the type with a second action, because the action contract can express neither.
      _Fix:_ Mint the id at the call site and pass it in (`createGame(id, name, typeId, players)`), or widen the create action to carry `typeId`. Then `goToGame` navigates to a known id and the second dispatch disappears. _(small)_
- [ ] **CR-081** `trackplay` · `trackplay/data/effects/trackplay.effects.ts:28` — The undo toast derives its label and target from `selectLastDeleted` instead of the action that triggered it, so a delete the reducer rejects pops a toast for an unrelated earlier deletion.
      _Fix:_ Take the name from the action payload (`ofType(...)` + `map(a => 'player' in a ? a.player.name : …)`), which also removes the state read and the `selectLastDeleted` export. Add the missing `trackplay.effects.spec.ts` — this is the domain's only hand-written effect and its behaviour is asserted nowhere but one e2e happy path. _(small)_
- [ ] **CR-082** `trackplay` · `trackplay/feature/game-play-page/game-play.page.scss:151` — The victory HUD's neon effects are not theme-gated, so the plain 'OK Boomer' theme renders rotating beams, a pulsing glow ring and animated confetti.
      _Fix:_ Wrap the `.tp-victory__*` glow/animation declarations in `:host-context([data-theme='cyberpunk'])` and leave a flat trophy + ranked list as the plain-theme base. _(small)_
- [ ] **CR-083** `settings` · `settings/data/settings.facade.ts:9` — Four doc-comments describe the slice as "the persisted schema `version` anchor + the theme", but `ISettingsState` has no version field and nothing reads a version from this slice.
      _Fix:_ Rewrite the four comments to say what the slice is: the app-global UI theme, eager because it must reach `<html data-theme>` under the boot splash. Fix the `@shared/util/migrations` path reference. Update docs/architecture.md:416 and :841-843 in the same pass — the "single anchor to read" sentence is the origin of the fiction. _(trivial)_
- [ ] **CR-084** `settings` · `settings/data/reducer/settings.reducer.ts:15` — Hydration replaces state wholesale (`settings ?? _state`) instead of merging over `initialSettings`, so an unchecked persisted doc can leave `theme` undefined and silently flip the app to the wrong theme.
      _Fix:_ `(state, { settings }) => ({ ...initialSettings, ...(settings ?? state) })`, matching office-time and trackplay. This is the slice with the largest blast radius per byte in the app and currently has the weakest hydration guard. _(trivial)_
- [ ] **CR-085** `settings` · `settings/data/effects/settings.effects.ts:24` — The boot splash's only real deadline is armed as an invisible side effect of this class injecting `SplashService`, and the comment documenting that coupling names a class that no longer exists.
      _Fix:_ Make the deadline self-standing rather than DI-incidental: arm it from `provideAppInitializer` inside `settingsContext` (or an `ENVIRONMENT_INITIALIZER` in `splash.service.ts`'s own provider) so it does not depend on who injects the service, and fix the comment to name `SettingsEffects`. While there: `revealSplash$`'s "Registered before revealSplash$ so data-theme is set before the splash lifts" (line 47) over-claims — both effects run synchronously inside the same `dispatch`, so no paint can occur between them and declaration order is not what makes it correct; folding the reveal into `applyTheme$`'s `tap` would make the ordering structural instead of asserted. _(small)_
- [ ] **CR-086** `settings` · `@shared/model/settings.types.ts:8` — `settings` is the only domain with no `model/` layer — its state type lives in `@shared/model` so the shared test kit can seed it, which is the exact arrangement the test kit itself documents as forbidden for domain types.
      _Fix:_ Move `ISettingsState` to `src/app/settings/model/settings.types.ts` and follow the dashboard precedent for the fixture: `mockSettings` moves to `settings/testing/settings.test-data.ts`, and `TMockKernelState` drops the `settings` key (grep shows no spec actually reads the seeded settings slice — the mocks that import `mockKernelState` are effects/selector specs for other domains). _(medium)_
- [ ] **CR-087** `geist` · `geist/feature/geist-page/geist.page.ts:153` — An in-flight `LanguageModel.create()` is neither cancelled nor reconciled when the page is destroyed, so navigating away during the multi-GB weights download leaves a live session that is never `destroy()`ed.
      _Fix:_ Hold one `AbortController` for session creation, pass its `signal` in `#sessionOptions()`, abort it in `#teardown()`, and on resolution destroy the session immediately if teardown already ran (`if (this.#destroyed) { session.destroy(); return; }`). _(small)_
- [x] **CR-088** **DONE** (78f90b1, the parallel session) — structurally closed by the CR-100 fix: `#readAvailability` now catches internally and always resolves, so `probe()` can no longer reject and there is nothing left to strand the page. `geist` · `geist/feature/geist-page/geist.page.ts:98` — A rejected availability probe strands the page on the `probing` panel forever and is memoized as a permanent failure, even though the state machine has a `flatlined` state for exactly this.
      _Fix:_ Catch in `#probeLink()` and set `link` to `'flatlined'` (the retry button already re-enters `prime()`); in the service, clear `#probe` on rejection so a retry actually re-probes rather than replaying the cached failure. _(trivial)_
- [x] **CR-089** **DONE** (625e840) `geist` · `@shared/util/language-model.service.ts:45` — The shared `availability` signal is never refreshed after a successful `createSession()`, so the deck keeps reporting GEIST as `standby` for the rest of the session once the user has actually downloaded the model.
      _Fix:_ Have `createSession()` set `#availability` to `'available'` on success (and drop the memoized `#probe` so a later probe re-reads), or expose a `refresh()` the geist page calls after `#openSession()` succeeds. _(trivial)_
- [ ] **CR-090** `geist` · `geist/feature/geist-page/geist.page.scss:147` — The transcript is a fixed-height scroll box that grows downward with no auto-scroll, so the streaming answer disappears below the fold after the first couple of turns.
      _Fix:_ Keep an `ElementRef` on `.gs-console` and pin `scrollTop = scrollHeight` from an `afterRenderEffect` keyed on `turns()`, honouring the usual "don't fight a user who scrolled up" check. _(small)_
- [ ] **CR-091** `geist` · `geist/util/transcript.utils.spec.ts:13` — The only spec in the domain covers two three-line pure helpers; the availability→link state machine, `canSend`, the `contextPercent` divide-by-zero guard and the abort/purge interplay — all of the domain's actual logic — are untested and structurally untestable.
      _Fix:_ Move the availability→`TGeistLink` map and the error→note mapping into `geist/util/` as pure functions (`linkForAvailability`, `noteForStreamError`) and spec them — two five-line specs — then add a thin `geist.page.spec.ts` over `canSend`/`contextPercent` with a stubbed `LanguageModelService`, matching the sibling pages' pattern. _(small)_
- [ ] **CR-092** `geist` · `geist/feature/geist-page/geist.page.ts:151` — `priming` conflates "downloading multi-GB weights" with "cheaply re-creating a session", and `primedPercent` is never reset, so routine actions flash a download panel and a retry starts the meter at a stale percentage.
      _Fix:_ Reset `primedPercent` to 0 at the top of `#openSession()`, and distinguish the two lifetimes — either a separate `reforging` link state that keeps the console mounted, or gate the panel on `primedPercent() > 0 || availability !== 'available'` so a cached re-create never shows the download UI. _(small)_
- [ ] **CR-160** `shared` · `@shared/feature/modal-dialog/base-modal-dialog.ts:40` — Every one of the seven `BaseModalDialog` subclasses hand-maintains its own `canSave` conjunction, so field validity is only correct as long as nobody forgets a clause — which is exactly what CR-016 is. The base declares `abstract readonly canSave: Signal<boolean>` and leaves the whole rule to each subclass.
      _Fix:_ Adopt **Signal Forms** (`@angular/forms/signals`). `form<TModel>(model: WritableSignal<TModel>)` takes a writable signal as its model and `draft` already is one (`linkedSignal<TForm>`), so `form(this.draft)` preserves the reseed-on-`existing()` behaviour the base exists for; `canSave` becomes tree validity instead of a hand-written `&&` chain, so a field cannot be silently omitted. Ionic is not a blocker — the `Field` directive binds to any component providing a `ControlValueAccessor`, which is what `@ionic/angular` ships for `ion-input`/`ion-select`/`ion-datetime` (long-term, `DateInputComponent` is the natural first control to convert to a real `FormValueControl`: it is `input`/`output` today where the contract wants `model()`). Not blocked on the Angular 22 bump — the v21→v22 export diff is purely additive and v21 is on the frozen `v21-lts` line, so the experimental tag carries no practical risk; see `open-tasks.md § Angular 22` for the evidence. **Do this last, after the rest of this list is worked off:** seven modals plus control bridging is too large to interleave with the remaining findings. Convert **one** dialog first as a spike — `transaction-edit-modal`, the one CR-016 already bit — and only then decide whether `BaseModalDialog` itself absorbs `form()`. Does **not** replace CR-016: fix that by hand now, this is what stops the class recurring. One caveat to check before planning: `TForm` is a view-model, not the entity (cents as a de-DE string, signed amount as magnitude + direction), so `eurToCents` parsing stays a custom `validate()` — the stable `transformedValue` API looks aimed at exactly that, but is unverified. `BaseEditItemDialog` gains little; those dialogs barely validate. _(large)_

## LOW

- [x] **CR-093** **DONE** (1abfded, landed inside the parallel session's fb46f1f — shared working tree) `repo` · `notifications/testing/notifications.test-data.ts:12` — Two entire testing modules (52 LOC) are dead — each file's sole export is referenced nowhere in src/ or e2e/.
      _Fix:_ Delete both files (and the now-empty testing/ folders), or wire them into the specs that currently hand-build the same state inline. Separately, drop the `export` keyword on the 42 own-file-only exports. _(trivial)_
- [x] **CR-094** **DONE** (af80396, prior to this sweep) `repo` · `.github/workflows/ci.yml:40` — CI enforces neither `format:check` nor template linting, so two of the repo's configured quality gates can only fail on a developer's machine.
      _Fix:_ Change the CI lint step to `pnpm exec eslint "src/**/*.{ts,html}"` and add a `pnpm run format:check` step; both are sub-10-second additions given the current clean state. _(trivial)_
- [ ] **CR-095** `repo` · `cash/feature/cash-rules-page/cash-rules.page.html:1` — Template duplication is 7.61% versus 0.45% in TypeScript — a 17× gap concentrated in cash, trackplay and groceries page shells.
      _Fix:_ The repo already has `@shared/ui/page-header/`; extend it to absorb the repeated toolbar preamble and adopt it in cash's five pages first — that alone retires ~7 of the 25 clones. _(small)_
- [x] **CR-096** **DONE** (1abfded) `repo` · `src/assets/i18n/de.json:251` — One i18n key is defined in both locale files but referenced nowhere in the source.
      _Fix:_ Delete the key from both files, or run `pnpm run i18n:extract` (which is already configured with `--clean`) and commit the diff. _(trivial)_
- [x] **CR-097** **DONE** (1abfded) `shared` · `@shared/ui/base-item/item-list-toolbar/item-list-toolbar.component.ts:30` — The toolbar's reorder feature is entirely dead — the input is never bound, the button is permanently hidden by a CSS class, and two specs assert the dead API.
      _Fix:_ Delete `showReorder`, `toggleReorder`, the hidden `ion-button`, and the two specs. If drag-reorder is still wanted (open-tasks.md "Rules drag-reorder"), it should be built with `ion-reorder-group` when it is built, not left as a stub. _(trivial)_
- [x] **CR-098** **DONE** (1abfded) `shared` · `@shared/ui/base-item/item-list-empty/item-list-empty.component.ts:29` — Three shared components call `addIcons({add, remove, cart, list})` while rendering no matching icons — the same dead-registration class the quality audit already cleared from the searchbar.
      _Fix:_ Delete the `addIcons` calls and the `ionicons/icons` imports from all three components; keep only registrations for icons the component's own template renders (as `page-header.component.ts:47-50` correctly does). _(trivial)_
- [ ] **CR-099** `shared` · `@shared/model/app.consts.ts:8` — Five kernel doc-comments point at files and planning documents that no longer exist, and one describes behaviour the file it names explicitly removed.
      _Fix:_ Repoint each reference at the current file (`persisted-slice.effects.factory`, `<domain>.providers.ts`, `SettingsEffects`) or at `docs/architecture.md §<n>` where the plan doc is gone; delete the `list.utils.ts:20` parenthetical outright. _(trivial)_
- [x] **CR-100** **DONE** (78f90b1, the parallel session) `shared` · `@shared/util/language-model.service.ts:33` — `probe()` has no error handling around an experimental browser API, so a throwing `LanguageModel.availability()` memoizes a rejected promise, pins the shared signal at `'probing'` forever, and produces an unhandled rejection from the constructor.
      _Fix:_ Wrap the call: `try { return await LanguageModel.availability(); } catch { return 'unavailable'; }`. That also makes the memoized promise always resolve, which is what the "probed once and shared" comment on line 22 already promises. _(trivial)_
- [ ] **CR-101** `shared` · `@shared/data/persisted-context.provider.spec.ts:47` — Several kernel specs assert framework behaviour or array lengths rather than the component's own logic.
      _Fix:_ For the descriptor, assert observable structure instead of length — e.g. that `hydrate:'boot'` adds a provider and empties `resolve` (already done), that omitting `save` yields one fewer effect than supplying it, and that two telemetry specs yield two distinct effect keys. Delete the three output-emission cases. _(trivial)_
- [ ] **CR-102** `shared` · `@shared/util/list/list-page.facade.ts:26` — `filterState` is on the shared facade contract even though all three implementations derive it identically from `state`, which the shell already has.
      _Fix:_ Drop `filterState` from `IListPageFacade`, compute it in `ListPageComponent` (`readonly filterState = computed(() => listStateFilter(this.facade.state()))`), and delete the three implementations. `categories` genuinely varies per domain and should stay. _(small)_
- [ ] **CR-103** `shared` · `@shared/feature/edit-categories-page/edit-categories.page.ts:57` — `FormsModule` is imported by the standalone manage-categories page but no form directive is used in its template.
      _Fix:_ Remove `FormsModule` from the `imports` array and its `@angular/forms` import. _(trivial)_
- [ ] **CR-104** `shared` · `@shared/ui/categories/category-note.directive.ts:18` — A directive with an `effect()` writes `textContent` and `style.display` imperatively to solve what is a two-line template conditional.
      _Fix:_ Replace the directive with a `computed` on `ListItemComponent` (`readonly categoryNote = computed(() => categoryNames(this.item(), this.categories()).join(', '))`) plus an `@if` in the template, and delete the directive and its spec. _(small)_
- [ ] **CR-105** `shared` · `@shared/data/actions/item-list.actions.factory.ts:20` — The shared item-list event map is adopted by two of the three list domains; tracking hand-writes nine of the same eleven events.
      _Fix:_ Spread the factory in `TrackingActions` too and accept the two unused `updateFilter`/`updateMode` creators (they cost nothing and tracking's facade already no-ops the category affordances), or make the factory take a `{ categories: boolean }` flag. Either way the map should have one definition. _(trivial)_
- [ ] **CR-106** `commlink` · `commlink/feature/commlink-page/commlink.page.ts:266` — `noise` re-derives the unread count by hand although the facade already exposes `notificationsUnread` backed by a dedicated, tested selector.
      _Fix:_ Delete `#comms` and `noise` and bind the template to `dashboard.notificationsUnread`. While there, decide on one read path: either `badge()` also goes through `telemetry(source)`, or `noise`/`nuyen`/`resonanceRating` read `#telemetry().bySource[...]` like `badge()` does — publishing both `dashboardState` and `telemetry(source)` on the facade for one consumer is one API too many. _(trivial)_
- [ ] **CR-107** `commlink` · `commlink/data/index.ts:12` — The same "why the dashboard is eager" rationale is restated in five source files plus architecture.md, and the copies have already drifted.
      _Fix:_ Keep the argument once — `commlink.providers.ts` is the natural home, since that is where `bootHydrationProvider` is chosen — and reduce the others to a one-line pointer. `index.ts` in particular should say what the barrel exports, not re-argue the lifecycle. _(trivial)_
- [ ] **CR-108** `commlink` · `commlink/feature/commlink-page/commlink.page.ts:99` — 128 lines of program catalog — the deck's documented configuration surface, including 13 hardcoded cross-domain route strings — live inside the page component with no test that any route resolves.
      _Fix:_ Move the array to `commlink/model/programs.ts` (or `commlink/model/programs.consts.ts`, matching the geist precedent) and add a spec that asserts every `route` matches a path in the exported route configuration — cheap now that each domain publishes a `routes/<domain>.routes.ts` manifest. _(small)_
- [ ] **CR-109** `commlink` · `commlink/feature/commlink-page/commlink.page.ts:280` — The deck's 1 Hz clock keeps ticking — and marking the deck subtree dirty — for the whole session, including while the user is on another page.
      _Fix:_ Drive the interval from Ionic's lifecycle (`ionViewDidEnter`/`ionViewWillLeave`) or gate it on an `isActive` signal, so the clock only runs while the deck is the visible page. Confirm the mount assumption first with a `ngOnDestroy` probe — if Ionic does destroy the view on root navigation, `toSignal`'s injection-context teardown already handles it and this is a non-issue. _(small)_
- [ ] **CR-110** `commlink` · `commlink/feature/commlink-page/commlink.page.scss:236` — An `offline` tile is "disabled" with `pointer-events: none`, which does not prevent keyboard activation, and the metric badge has no accessible name.
      _Fix:_ Bind `[attr.aria-disabled]="nodeStatus === 'offline'"` and `[tabindex]="nodeStatus === 'offline' ? -1 : 0"` (or drop the `routerLink` for offline programs) alongside the CSS, and give the badge an `[attr.aria-label]` describing the metric. _(trivial)_
- [ ] **CR-111** `commlink` · `commlink/feature/commlink-page/commlink.page.html:27` — The app's home page is entirely hardcoded English and invisible to `i18n:extract`, in a German-default app where every other page routes copy through `translate`.
      _Fix:_ Codenames (CHRONO, SOYKAF) are deliberate flavour and should stay literal, but move the `desc` strings and the chrome labels behind `marker('commlink.*')` keys so the copy is at least addressable. If leaving it English is a product decision, record it in `open-tasks.md § Deliberately not abstracted` so it stops reading as an oversight. _(small)_
- [ ] **CR-112** `tracking` · `tracking/model/tracking.types.ts:19` — The comment claiming tracking "is the only reader" of its `TTrackingCommand` literals is false — the notifications page switches on the same three strings — and `tracking.stop` is never produced by anything.
      _Fix:_ Either put the label key on the projected row (the producer owns its vocabulary and hands over presentational data, consistent with the `toast` contract), or delete `tracking.stop` and correct the comment to say the inbox also interprets the literals for labelling. _(small)_
- [ ] **CR-113** `tracking` · `tracking/data/selectors/tracking.selector.ts:88` — The per-second `updateTracking` tick forces a full 21-day stacked bar chart rebuild, because `selectAllTrackingSessions` returns a fresh array on every tracking-state change.
      _Fix:_ Split the live and archived reads: have the chart selector take `selectTrackingState`'s `sessions` plus only the running item's day bucket, or memoise on a coarser key (e.g. rounded minutes) so a second's worth of drift doesn't invalidate the whole series. _(medium)_
- [x] **CR-114** **DONE** (dc162eb) `tracking` · `tracking/data/tracking.providers.ts:36` — `seedDemoSessions` mutates the persisted `sessions` array but is absent from the save trigger, so seeded data is wiped by the next route entry's re-hydration.
      _Fix:_ Add `TrackingActions.seedDemoSessions` to the save trigger list. One line, and it makes the dev affordance actually usable. _(trivial)_
- [x] **CR-115** **DONE** (dc162eb) — both halves: `TrackingTimePipe` now delegates, and `TimeFromSecondsPipe`/`time-from-seconds.pipe.ts` renamed to `MinutesFromSecondsPipe`/`minutes-from-seconds.pipe.ts`. `tracking` · `tracking/util/tracking-time.pipe.ts:11` — `TrackingTimePipe` re-implements `formatSecondsAsClock` — including its own `pad` — instead of calling the util one folder over, and only the util is spec'd.
      _Fix:_ `transform(value?: IDataItem) { return formatSecondsAsClock(value?.trackedTimeInSeconds ?? 0); }` and delete the local `pad`. Same for the naming lie next door: `TimeFromSecondsPipe` (`time-from-seconds.pipe.ts:8`) returns a bare minute count, not a time — `minutesFromSeconds` is what it does. _(trivial)_
- [x] **CR-116** **DONE** (dc162eb) `tracking` · `tracking/ui/tracking-item/tracking-item.component.html:90` — Every tracking row renders an `ion-reorder` drag handle that cannot work — there is no `ion-reorder-group` anywhere in the render path.
      _Fix:_ Remove the `ion-reorder` and its import, or wire an `ion-reorder-group` with an `ionItemReorder` handler and a `reorderItems` action. The same dead handle exists in `@shared/ui/base-item/list-item` and `categories/category-item`, so this is worth one sweep. _(trivial)_
- [x] **CR-117** **DONE** (dc162eb) `tracking` · `tracking/data/reducer/tracking.reducer.ts:78` — `resetTracking` shallow-clones every item it does *not* reset, handing downstream selectors new references to recompute from for no state change.
      _Fix:_ `return listItem;` for the untouched branch. _(trivial)_
- [x] **CR-118** **DONE** (7cb06c6) `office-time` · `office-time/feature/office-time-settings-page/office-time-settings.page.html:43` — A hardcoded German button label survives in an otherwise fully translated domain, and its confirm copy overstates what the reset actually clears.
      _Fix:_ Add `officetime.page.settings.reset.button` to de/en and bind it; reword the alert header/subheader to name the scope ("Bürozeiten-Daten zurücksetzen"). _(trivial)_
- [ ] **CR-119** `office-time` · `office-time/model/office-time.types.ts:21` — The domain's i18n namespace is `officetime.`, which matches neither the domain folder nor the route, and it also hosts another domain's keys.
      _Fix:_ Rename the group to `office-time.` in one pass (de + en + templates + the `marker(...)` calls) and move the four barcode keys to a `barcode.` group owned by that domain. _(small)_
- [x] **CR-120** **DONE** (93edf13) `notifications` · `notifications/util/notifications.transforms.ts:10` — Two comments in the transforms file describe an architecture that was deleted, which is the failure mode this repo's comment rule is meant to prevent.
      _Fix:_ Delete both comments (the code is self-explanatory) or rewrite the docstring to state the one non-derivable rule that remains: callers pass `now` so the transforms stay pure. _(trivial)_
- [x] **CR-121** **DONE** (93edf13) `notifications` · `notifications/data/reducer/notifications.reducer.ts:69` — The `loaded` handler carries a defensive backfill for a schema state that cannot exist in this app, and its spec asserts that impossible state via a double `as never` cast.
      _Fix:_ Delete the backfill and the spec; if a hop is ever needed, declare it as a `ladder` step where the other contexts declare theirs. _(trivial)_
- [x] **CR-122** **DONE** (93edf13) `notifications` · `notifications/data/actions/notifications.actions.ts:12` — `NotificationsListActions` is not a list-engine group — it is the inbox's lifecycle and view state — and the name collides with the app's `list` vocabulary while diverging from its own sibling precedent.
      _Fix:_ Rename to `NotificationsInboxActions` (file stays `actions/notifications.actions.ts`); it is a mechanical rename confined to five files. _(trivial)_
- [x] **CR-123** **DONE** (93edf13) — un-exported `patchNotificationById`/`isUnread`; the third symbol wasn't found on re-check, but the naming duplicate is resolved (moved into the reducer as `initialNotificationsState`, `EMPTY_NOTIFICATIONS_STATE` retired). `notifications` · `notifications/util/notifications.transforms.ts:86` — Three exported symbols are only ever used inside their own file, and the reducer re-exports the initial state under a second name.
      _Fix:_ Drop the `export` on `patchNotificationById` and `isUnread`; pick one name for the empty state (keep `EMPTY_NOTIFICATIONS_STATE` in util and pass it straight to `createReducer`, or move it into the reducer since nothing else reads it). _(trivial)_
- [x] **CR-124** **DONE** (98b6419, prior to this sweep) — `nextDailyOccurrence` already extracted to `notification-schedule.utils.ts` with a full spec (before/exactly-at/after/DST); the doc had just never caught up. `notifications` · `notifications/util/notification.service.ts:68` — The only real logic in `NotificationService` — the next-occurrence computation — is untested and unextractable as written, because it is a private method wedged between plugin calls.
      _Fix:_ Extract `nextDailyOccurrence(hour, minute, now = dayjs()): Date` as an exported pure function in `notifications/util` and spec the three cases (before, exactly at, after). The plugin wrapper then has no logic left to test. _(trivial)_
- [x] **CR-125** **DONE** (93edf13) `notifications` · `notifications/feature/notifications-page/notifications.page.html:65` — The done-section disclosure is a button with no `aria-expanded`, so screen-reader users get no state and no hint that activating it reveals content.
      _Fix:_ Add `[attr.aria-expanded]="!collapsed"` and an `aria-controls` pointing at an id on the done list region. _(trivial)_
- [x] **CR-126** **DONE** (2d1e38a) `barcode` · `barcode/data/index.ts:14` — The barrel's docblock claims consumers that do not exist: `selectBarcodeDataUrl` has zero importers outside `barcode/data`, and `BarcodeActions` has exactly one — a spec file.
      _Fix:_ Reduce the barrel to `BarcodeFacade` + `barcodeContext` (matching `settings/data`). For the spec, assert on `store.dispatch` receiving a data-URL payload via a `BarcodeFacade` spy rather than needing the action creator exported. _(trivial)_
- [x] **CR-127** **DONE** (2d1e38a) `barcode` · `barcode/feature/barcode-page/barcode.page.html:12` — The destructive delete is a single unconfirmed tap sitting directly beside the rotate button, with no undo.
      _Fix:_ Put delete behind an `AlertController` confirm, or separate the two buttons and demote delete to a header/overflow action. _(trivial)_
- [x] **CR-128** **DONE** (2d1e38a) `barcode` · `barcode/feature/barcode-page/barcode.page.html:10` — The badge image carries a hardcoded English `alt`, and the file input's `aria-label` is attached to an element that is not in the accessibility tree.
      _Fix:_ Bind the alt to an i18n key (`'barcode.image.alt' | translate` once the keys move out of `officetime.*`) and drop the dead `aria-label` from the hidden input. _(trivial)_
- [x] **CR-129** **DONE** (2d1e38a) `barcode` · `barcode/data/actions/barcode.actions.ts:14` — `saveBarcode(base64Blob)` names a data URL as a blob of bare base64; the reducer immediately renames the same value `dataUrl`.
      _Fix:_ Rename the payload field to `dataUrl` for parity with `rotateBarcodeSuccess` and `IBarcodeState.dataUrl`. Three call sites. _(trivial)_
- [x] **CR-130** **DONE** (2d1e38a) `barcode` · `CLAUDE.md:180` — CLAUDE.md — the document that exists to keep the two unrelated "barcode" features apart — points at a path for the mlkit scanner that does not exist.
      _Fix:_ Update the path in CLAUDE.md and note that the scanner is grocery-owned (it feeds `showCreateProductFromScan`), which is also why it is not in `@shared`. _(trivial)_
- [x] **CR-131** **DONE** (8099f65) `tasks` · `tasks/util/task.utils.ts:9` — `isTaskItem` has no production caller — its only consumer is its own spec, and its docstring explains exactly why nothing needs it any more.
      _Fix:_ Delete `task.utils.ts` and its spec. _(trivial)_
- [x] **CR-132** **DONE** (8099f65) — dropped the two dead selector exports; left the `TasksActions` question alone per the app-wide open decision. `tasks` · `tasks/data/index.ts:16` — The facade barrel is not minimal: two of its five exports have zero importers anywhere, and a third is reachable only from spec files.
      _Fix:_ Drop the two selector re-exports; decide the `TasksActions` question the same way for all eleven domains (the memory's open "test-only Actions exports" decision) rather than leaving it per-domain. _(trivial)_
- [x] **CR-133** **DONE** (8099f65) — verified the key survives `i18n:extract` via the template's own literal usage. `tasks` · `tasks/data/effects/tasks-list.effects.ts:11` — A bare `marker('list-header.tasks')` sits at module scope in the effects file, for a key used only in a template that already extracts it via the translate pipe.
      _Fix:_ Delete line 11 and the now-unused `marker` import; verify with `pnpm run i18n:extract` that the key survives (it does for tracking). _(trivial)_
- [x] **CR-134** **DONE** (8099f65) `tasks` · `tasks/feature/tasks-page/tasks.page.html:9` — Four i18n keys used exclusively by the tasks domain still live under the kitchen-bot `grocery.` namespace.
      _Fix:_ Rename to `tasks.item.prio` / `tasks.item.due.note` / `tasks.list-toolbar.{prio,due}` in `de.json` + `en.json` and the two templates — the same move that retired the `officetime.` and `cash.` title prefixes. _(trivial)_
- [x] **CR-135** **DONE** (8099f65) `tasks` · `tasks/data/effects/tasks-list.effects.ts:24` — `addItemFailure` is emitted on a duplicate add but nothing in tasks handles it, so pressing Enter on an existing task name does nothing at all, silently.
      _Fix:_ Either add a one-line effect mapping `TasksActions.addItemFailure` to `NotificationsActions.toast({ key: 'item-list.duplicate', params: { name } })` (mirroring `tracking-message.effects.ts`), or stop emitting the action and let the shared factory return nothing. _(trivial)_
- [x] **CR-136** **DONE** (2a7a56b, prior to this sweep) — verified: selector is already `app-page-tasks`, and `e2e/tasks/tasks.e2e.ts` already exists; the doc just never caught up. `tasks` · `tasks/feature/tasks-page/tasks.page.ts:19` — `selector: 'app-page-task'` is singular — the only page selector in the app that does not match its folder, domain and route, and the repo's e2e scoping rule keys off exactly that string.
      _Fix:_ Rename the selector to `app-page-tasks`, move the spec to `e2e/tasks/tasks.e2e.ts`, and scope its locators to `page.locator('app-page-tasks')`. _(trivial)_
- [ ] **CR-137** `cash` · `cash/model/transaction.types.ts:14` — `rawDescription` is documented as 'Original bank text (kept verbatim from import)' but both parsers store a derived, em-dash-joined string.
      _Fix:_ Either store the untouched source line (or the concatenated source fields) in `rawDescription` and keep `description` as the display join, or drop the field and update the type comment and `descriptionText` to say the two are the same. _(small)_
- [x] **CR-138** **DONE** (0a01f7b, prior to this sweep) — verified: `CashActions.enterPage` no longer exists; the doc just never caught up. `cash` · `cash/data/actions/cash.actions.ts:20` — `CashActions.enterPage` is declared on the published action contract but is never dispatched or consumed anywhere in the app.
      _Fix:_ Delete the action. _(trivial)_
- [x] **CR-139** **DONE** (abb10e6) `cash` · `cash/feature/cash-page/cash.page.ts:147` — `protected readonly trashOutline = trashOutline;` is unused in both pages that declare it — the templates reference the icon by string name.
      _Fix:_ Remove both fields. _(trivial)_
- [x] **CR-140** **DONE** (abb10e6) `cash` · `cash/feature/cash-page/cash.page.html:65` — The three destructive swipe-to-delete options are the only icon-only cash controls without an accessible label.
      _Fix:_ Add `cash.a11y.delete-account` / `-rule` / `-transaction` keys (de + en) and bind them like the reconcile/detach options. _(trivial)_
- [x] **CR-141** **DONE** (abb10e6) `cash` · `cash/util/money.utils.ts:21` — `eurToCents` rejects a minus sign that follows the currency symbol, contradicting its own docstring.
      _Fix:_ Strip `€`/whitespace first, then extract a leading *or* trailing sign, and extend the spec table with both forms — or narrow the docstring to say the sign must lead. _(trivial)_
- [x] **CR-142** **DONE** (abb10e6) `cash` · `cash/data/index.ts:15` — The data barrel re-exports fourteen selectors that no code outside `cash/data` imports, contradicting the 'facade-only surface' it documents.
      _Fix:_ Either trim the barrel to what is actually imported (`CashActions`, `CashFacade`, `CashCategoriesPageFacade`, `cashContext`, `TAccountTxn`) or, if the app-wide convention is deliberate, say so in the docblock instead of claiming a minimal surface. _(trivial)_
- [x] **CR-143** **DONE** (54e57d7) `trackplay` · `trackplay/feature/games-page/games.page.ts:95` — A hardcoded German fallback label `'Unbekannt'` is duplicated across two pages and pinned by a spec.
      _Fix:_ Add `trackplay.label.unknown-type` to de/en and extract the shared lookup (both pages need the identical `typeName(game)`), e.g. as a small pure helper in `trackplay/util` or a pipe over the types map. _(trivial)_
- [x] **CR-144** **DONE** (4e7c9da, prior to this sweep — the parallel review batch) `trackplay` · `trackplay/feature/game-play-page/game-play.page.ts:121` — Every cell blur dispatches `setRoundValue` even when the value did not change, bumping the game's `updated` and every participant's `lastPlayed` and re-persisting the whole slice.
      _Fix:_ Early-return in the reducer when `round.values[playerId] === value` (cheap, also fixes any other caller), or compare against `valueFor(round, pid)` before dispatching. _(trivial)_
- [ ] **CR-145** `trackplay` · `trackplay/ui/game-list-item/game-list-item.component.ts:65` — The same swipe-to-delete/edit skeleton is copy-pasted across all three list-item components.
      _Fix:_ Extract the behaviour into a small `SwipeActionsDirective`/base class in `trackplay/ui` (inputs: ionList, canDelete; outputs: delete, edit), or a shared one if `@shared/ui/base-item/list-item` can host it without dragging in `IBaseItem`. _(small)_
- [x] **CR-146** **DONE** (54e57d7) `trackplay` · `trackplay/smart-ui/game-settings-popover/game-settings-popover.component.ts:52` — `mode` is a plain mutable field read inside `computed()`, so the computeds are only correct by accident — and the component's name claims it edits game settings when it also owns the players-list config.
      _Fix:_ Make `mode` a `signal` behind a setter like every other componentProps-driven dialog, and rename the component (and folder) to something honest such as `list-settings-popover`. _(trivial)_
- [x] **CR-147** **DONE** (54e57d7) `trackplay` · `trackplay/data/actions/trackplay.actions.ts:14` — Comments name symbols that no longer exist and describe a naming scheme the file itself abandoned.
      _Fix:_ Delete the stale references; if the load/loaded-exclusion note is worth keeping, state the rule ('the generic save effect excludes the load/loaded lifecycle') without naming a foreign symbol. _(trivial)_
- [ ] **CR-148** `settings` · `settings/feature/settings-page/settings.page.html:15` — The picker's theme values are untyped template literals cast through `$any`, so nothing checks them against `TTheme` or checks that every member has a button.
      _Fix:_ Export `const THEMES = ['cyberpunk', 'boomer'] as const satisfies readonly TTheme[]` next to `TTheme`, `@for` over it in the template with `'settings.theme.' + theme | translate`, and type the handler off Ionic's `SegmentCustomEvent` instead of `$any`. Adding a theme then fails to compile until the union is extended. _(small)_
- [ ] **CR-149** `settings` · `settings/feature/settings-page/settings.page.html:9` — The theme picker has no accessible name — its visible label sits in a separate `ion-item` with no programmatic association.
      _Fix:_ Give the label an `id` and add `[attr.aria-labelledby]` on the `ion-segment` (or drop the wrapper item and use `<ion-label position="stacked">` inside the same `ion-item` as the segment). _(trivial)_
- [x] **CR-150** **DONE** (93edf13, folded into the CR-056 fix) `settings` · `src/assets/i18n/de.json:340` — The `settings.` i18n namespace contains keys the settings domain never renders — they belong to the notifications debug panel.
      _Fix:_ Rename to `notifications.debug.*` in both bundles and update the two template references. _(trivial)_
- [x] **CR-151** **DONE** (625e840) `geist` · `geist/feature/geist-page/geist.page.html:128` — The streamed answer is not announced to assistive tech (no live region) and the persona register's selected state is conveyed by CSS class only.
      _Fix:_ Put `role="log" aria-live="polite"` on `.gs-console`, `aria-hidden="true"` on the `gs-cursor` span and the brand icon, and `[attr.aria-pressed]="p.id === persona().id"` on the persona buttons. _(trivial)_
- [x] **CR-152** **DONE** (625e840) `geist` · `@shared/util/language-model.service.ts:24` — `LanguageModelService.isUsable` has no consumers anywhere in the app or e2e suite.
      _Fix:_ Delete it; re-add when a caller exists. _(trivial)_
- [x] **CR-153** **DONE** (625e840) — dropped the four attributes rather than promoting a shared `.sr-glitch` utility. `geist` · `geist/feature/geist-page/geist.page.html:36` — Four `data-text` attributes on the panel titles are inert — the only rule that reads `attr(data-text)` is scoped to another component's encapsulated styles.
      _Fix:_ Either drop the four attributes, or — since the glitch is a deck signature used on two pages now — promote commlink's rule into a `.sr-glitch` utility in `_shadowrun.scss` and apply the class here. _(trivial)_
- [x] **CR-154** **DONE** (625e840) `geist` · `geist/model/geist.types.ts:28` — Three i18n-key fields are typed `string` and explained by a comment, where the repo already has `TMarker` to say it in the type.
      _Fix:_ Type `note`, `taglineKey`, `GEIST_LINK_LABELS`'s value and `#noteFor`'s return as `TMarker` and delete the comment on line 28. _(trivial)_
- [x] **CR-155** **DONE** (625e840) `geist` · `geist/feature/geist-page/geist.page.html:43` — `< BACK TO DECK` is the domain's one actionable control with a hardcoded English label while every other control uses a `geist.action.*` key.
      _Fix:_ Add a `geist.action.back-to-deck` key and bind it through the translate pipe like the sibling buttons. _(trivial)_
- [ ] **CR-156** `shell` · `src/index.html:23` — The favicon link points at `assets/icon/favicon.png`, a path that exists in neither `src/assets/` nor the build output, so every page load 404s on it.
      _Fix:_ Ship a favicon and point the link at it. `ngsw-config.json:10` already prefetches `/favicon.ico`, so `public/favicon.ico` + `<link rel="icon" href="favicon.ico">` satisfies both references at once and needs no new asset entry (`public/**` is copied to the output root). _(trivial)_
- [ ] **CR-157** `shell` · `ngsw-config.json:10` — The prefetch asset group lists `/favicon.ico`, which no build output produces.
      _Fix:_ Same fix as CR-156 — add the file rather than removing the glob. Harmless as it stands (the ngsw build drops globs that match nothing when it generates `ngsw.json`), but it documents an asset the app does not have. _(trivial)_
- [ ] **CR-158** `shell` · `src/assets/avatar.svg:1` — `avatar.svg` and `shapes.svg` are Ionic starter leftovers, referenced from nowhere in `src/` or `e2e/`, and shipped on every build.
      _Fix:_ Delete both files. _(trivial)_
- [ ] **CR-159** `shell` · `angular.json:31` — The build declares two static-asset lanes (`src/assets → /assets`, `public → /`) where current Angular declares one. Both are verbatim copies — neither is hashed or processed — so the second lane buys only a URL namespace, and once CR-158 and the holiday fixture (`open-tasks.md`) land, `src/assets/` holds nothing but the two i18n bundles.
      _Fix:_ Collapse onto `public/`, the layout `ng new` has produced since v17.3: move `src/assets/i18n/` to `public/i18n/`, change the loader prefix in `src/main.ts:49` to `'./i18n/'`, repoint the `ngsw-config.json:26` asset group at `/i18n/*.json`, and delete the `src/assets` entry from `angular.json`. Nothing else in the repo names `/assets/` — no spec, no e2e, no Playwright config — and `src/index.html:23` is already being repointed by CR-156. Worth doing as one sweep with CR-156/158: all three touch `index.html`, `ngsw-config.json` and the asset lanes. The rule then reads: fetched by URL or hardcoded by something outside the build → `public/`; **imported** by code → next to that code under `src/app/`, where the bundler hashes it and TypeScript can see it. _(small)_