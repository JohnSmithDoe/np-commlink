/**
 * This file includes polyfills needed by Angular and is loaded before the app.
 *
 * The app runs zoneless (see `provideZonelessChangeDetection()` in main.ts),
 * so zone.js is intentionally NOT imported here. Add any browser polyfills
 * your target platforms require below.
 *
 * Learn more in https://angular.dev/guide/zoneless
 */

export {};
